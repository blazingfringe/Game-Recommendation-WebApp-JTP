import { Modal, styled } from '@mui/material'

/**
 * Styled Mui Elements
 */

export const StyledModal = styled(Modal)({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
})